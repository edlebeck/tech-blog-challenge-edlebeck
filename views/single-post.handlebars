{{> post-details post}}


{{> comments post.comments}}
{{#if loggedIn}}
<form class="comment-form container col-3 justify-content-center">
    <div>
        <textarea name="comment-body"></textarea>
    </div>

    <div>
        <button type="submit" class="btn btn-dark m-3">Add Comment</button>
    </div>
</form>
{{/if}}

{{#if loggedIn}}
<script src="/js/add-comment.js"></script>
{{/if}}